<Window x:Class="Telltale_Script_Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Telltale_Script_Editor"
        mc:Ignorable="d"
        Title="Telltale Script Editor" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File" Height="25">
                    <MenuItem Header="New">
                        <MenuItem Header="Project"/>
                        <MenuItem Header="Script"/>
                    </MenuItem>
                    <MenuItem Header="Open">
                        <MenuItem Header="Project"/>
                        <MenuItem Header="TTARCH2 Archive"/>
                    </MenuItem>
                    <MenuItem Header="Save"/>
                    <MenuItem Header="Save As"/>
                    <MenuItem  Header="Exit"/>
                </MenuItem>
                <MenuItem Header="Edit" Height="25">
                    <MenuItem Header="Undo"/>
                    <MenuItem Header="Redo"/>
                </MenuItem>
                <MenuItem Header="Project" Height="25">
                    <MenuItem Header="Build"/>
                    <MenuItem Header="Build and Run"/>
                </MenuItem>
                <MenuItem Header="Help" Height="25">
                    <MenuItem Header="Docs"/>
                    <MenuItem Header="About"/>
                    <MenuItem Header="Contribute"/>
                    <MenuItem Header="Debug">
                        <MenuItem Header="Verbose Output" IsCheckable="true" IsChecked="True"/>
                        <MenuItem Header="Show Project Info"/>
                    </MenuItem>
                </MenuItem>
            </Menu>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" materialDesign:ShadowAssist.ShadowEdges="Right" materialDesign:ShadowAssist.Darken="True">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>
                    <TreeView x:Name="editorTreeView"  Grid.Row="0"/>
                    <GridSplitter Height="5" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" ResizeBehavior="CurrentAndNext" Opacity="0.5"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Visible" Grid.Row="1">
                        <TextBox Text="Console output will appear here.&#x0a;Enable verbosity under Help -> Verbose Output." TextWrapping="NoWrap" IsReadOnly="true" AcceptsReturn="True" materialDesign:TextFieldAssist.DecorationVisibility="Hidden"  BorderThickness="0"/>
                    </ScrollViewer>
                </Grid>
                <GridSplitter Width="5" HorizontalAlignment="Right" VerticalAlignment="Stretch" ResizeBehavior="CurrentAndNext" Opacity="0.5"/>
                <ScrollViewer Background="{DynamicResource MaterialDesignPaper}" Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Visible">
                    <TextBox Background="{DynamicResource MaterialDesignLightBackground}" Text="This is a test of the textbox. Not sure why it wouldn't work, but I'm going to test it anyway. Why not?" AcceptsReturn="True" AcceptsTab="True" materialDesign:TextFieldAssist.DecorationVisibility="Hidden" BorderThickness="0"/>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
